"""Core model definitions.

Submodules are imported fully-qualified by the analysis code
(e.g. ``from sparse_pretrain.src.model import SparseGPT``), so this package
initialiser is intentionally side-effect free.
"""
