"""Node-mask circuit pruning (Gao et al. 2025, Section 2.2 / Appendix A.5).

Submodules are imported fully-qualified by the analysis code
(e.g. ``from sparse_pretrain.src.pruning.tasks import get_task``), so this
package initialiser is intentionally side-effect free.
"""
