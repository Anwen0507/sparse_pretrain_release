"""Experiment and plotting scripts.

Each script is runnable as a module, e.g.::

    python -m sparse_pretrain.scripts.universality_pruning_experiment --help

A handful of them (``motif_dictionary``, ``atom_excision``, ``epistasis_test``,
``pronoun_split``, ``iteration_jaccard``, ...) are also imported as libraries by
the others, which is why this directory is a package.
"""
